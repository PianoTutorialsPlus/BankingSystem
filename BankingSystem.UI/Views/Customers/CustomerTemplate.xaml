<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:viewModels="clr-namespace:BankingSystem.UI.ViewModels.Customers">

    <DataTemplate DataType="{x:Type viewModels:AbstractEditCustomerViewModelBase}">
        <Grid Margin="20" Width="450">
            <StackPanel>
                <TextBlock Text="First Name" />
                <TextBox Text="{Binding Customer.FirstName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" />

                <TextBlock Text="Last Name" />
                <TextBox Text="{Binding Customer.LastName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" />

                <TextBlock Text="Street" />
                <TextBox Text="{Binding Customer.Street}" Margin="0,0,0,10" />

                <TextBlock Text="House Number" />
                <TextBox Text="{Binding Customer.HouseNumber}" Margin="0,0,0,10" />

                <TextBlock Text="Zip Code" />
                <TextBox Text="{Binding Customer.ZipCode}" Margin="0,0,0,10" />

                <TextBlock Text="City" />
                <TextBox Text="{Binding Customer.City}" Margin="0,0,0,10" />

                <TextBlock Text="Phone Number" />
                <TextBox Text="{Binding Customer.PhoneNumber}" Margin="0,0,0,10" />

                <TextBlock Text="Email Address" />
                <TextBox Text="{Binding Customer.EmailAddress}" Margin="0,0,0,10" />

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="Save" Command="{Binding SaveCommand}" Width="100" Margin="0,0,10,0" />
                    <Button Content="Cancel" Command="{Binding CancelCommand}" Width="100" IsCancel="True"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </DataTemplate>

    <ControlTemplate x:Key="ValidationErrorTemplate">
        <StackPanel>
            <Border BorderBrush="Red" BorderThickness="2" CornerRadius="3">
                <AdornedElementPlaceholder x:Name="placeholder" />
            </Border>
            <ItemsControl ItemsSource="{Binding ElementName=placeholder, 
                                     Path=AdornedElement.(Validation.Errors)}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Foreground="Red" FontSize="11" Text="{Binding ErrorContent}" Margin="2,0,0,0"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ControlTemplate>

    <Style TargetType="TextBox">
        <Setter Property="Validation.ErrorTemplate" Value="{StaticResource ValidationErrorTemplate}" />
    </Style>

</ResourceDictionary>

